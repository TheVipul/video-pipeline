# Core YouTube pipeline
yt-dlp>=2024.10.7
bgutil-ytdlp-pot-provider>=1.3.0
# bgutil-ytdlp-pot (the actual PO token server binary) is installed separately

# LLM (OpenAI-compatible -> MiniMax)
openai>=1.54.0

# LangGraph orchestration agent
langgraph>=0.2.50
langchain>=0.3.7
langchain-openai>=0.2.0
langchain-core>=0.3.21

# Config & data
pydantic>=2.9.2
pydantic-settings>=2.6.1
python-dotenv>=1.0.1
PyYAML>=6.0.2

# HTTP & async
httpx>=0.27.2
aiofiles>=24.1.0

# Publishing targets
boto3>=1.35.0                      # S3 / MinIO / R2 / B2 re-upload
google-api-python-client>=2.149.0  # YouTube Data API v3 re-upload
google-auth-oauthlib>=1.2.1

# Media processing
ffmpeg-python>=0.2.0
Pillow>=11.0.0

# Output & observability
rich>=13.9.4
structlog>=24.4.0
jinja2>=3.1.4

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
moto[server]>=5.0.0                # real in-process S3 server for publisher tests
json-repair>=0.30.0                # recovers malformed JSON from LLM tool output
